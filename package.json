{
    "name": "api",
    "private": true,
    "scripts": {
        "dev": "UVICORN_RELOAD_DIRS=src uvicorn src.main:app --reload --host 127.0.0.1 --port 8000",
        "build": "echo no-build",
        "start": "uvicorn src.main:app --host 0.0.0.0 --port 8000"
    }
}